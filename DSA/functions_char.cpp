#include <bits/stdc++.h>
using namespace std;
int main(){
//INBUILT FUNCTION FOR CHAR ARRAY
    //to find length we can use the function strlen(char_array)
    // to compare two strings s1 and s2 we use strcmp(s1, s2) and it will give either not equal to 0 when its not 0 else 1
    // to copy a string we use strcopy(dest, source) copies from source to dest


//INBUILT FOR STRING ARE IN STRINGS.CPP
    // str.length() 
    // str.compare()
    //etc.....
    return 0;
}
