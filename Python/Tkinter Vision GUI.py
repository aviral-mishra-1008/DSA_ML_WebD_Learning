import tkinter 
window = tkinter.Tk()
window.title("First Program on GUI")
label = tkinter.Label(window, text = "Hi User!").pack()
window.mainloop()
#the label is the text to be displayed while title is the heading of the window
#window.mainloop() is command used for keeping the window open as long as the user needs

#adding widgets
#They help us interact with GUI interface

#Label is used above, it is used to enter a line or text anywhere


