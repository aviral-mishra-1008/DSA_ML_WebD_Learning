const age = 12;
//here's same format as JAVA
if(age>=18){
    console.log("You are eligible!");
}

else if(age==18){
    console.log("Congratulations for your first votign year!");
}

else{
    console.log("You cannot vote!");
}

//TERNARY OPERATOR

const marks = 50;

const result = (marks>=40)?"Passed":"Failed";  //so the format for ternary operator is (condition)?"What to do if true":"What to do if false";
//So, using const we cannot as result type when using if-else and thus we have ternary operator;


//SWITCH CASE 

