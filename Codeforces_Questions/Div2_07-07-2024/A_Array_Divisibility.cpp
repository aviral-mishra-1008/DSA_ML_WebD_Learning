#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        set<int> divisible; 
        for(int i=1; i<=n; i++){
            divisible.insert(i);
        }
        
        vector<int> ans; 
        
    }
}