#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        vector<int> checker(n,0);
        int sum = 0;
        for(int i=1;i<n;i++){
            int addSum = 0;
            if(checker[i]!=0){
                addSum = checker[i];
            }
        }
    }

    return 0;
}